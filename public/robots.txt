# robots.txt for mohakash.xyz
#
# Policy: this site is public and meant to be FOUND and CITED — by search
# engines and by AI assistants (ChatGPT, Claude, Perplexity, Google AI
# overviews). We allow AI assistants to FETCH and CITE pages live (retrieval),
# but we do NOT allow our content to be used to TRAIN models. The only path
# kept private is the CMS admin route.
#
# NOTE: For this file to actually be served, Cloudflare's "managed robots.txt"
# / AI-bot blocking must be DISABLED in the dashboard — otherwise Cloudflare
# prepends its own block (Disallow: / for AI bots) above these rules and also
# returns 403 to AI crawlers at the edge, overriding everything here.
#
# The bot -> purpose mapping below is a moving target (vendors rename/split
# their crawlers); revisit periodically.

# --- Default: allow all well-behaved crawlers ---
# Content-Signal expresses intent for crawlers that honor it:
#   search   = yes  -> may index for search results
#   ai-input = yes  -> may fetch for live retrieval / RAG / citation
#   ai-train = no   -> may NOT use content to train/fine-tune models
User-agent: *
Content-Signal: search=yes, ai-input=yes, ai-train=no
Allow: /
Disallow: /admin

# --- AI retrieval / live-fetch / search-index bots (ALLOWED, so assistants
#     can fetch and cite the site) ---
User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Perplexity-User
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: anthropic-ai
Allow: /

User-agent: Applebot
Allow: /

# --- AI training-only crawlers (DISALLOWED, so content isn't used to train
#     models) ---
User-agent: GPTBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: Amazonbot
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: cohere-ai
Disallow: /

# Machine-readable site guide for LLMs (https://llmstxt.org) — see /llms.txt

Sitemap: https://mohakash.xyz/sitemap-index.xml
